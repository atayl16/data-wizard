<%= form_with(model: petcategory, local: true) do |form| %>
  <% if petcategory.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(petcategory.errors.count, "error") %> prohibited this petcategory from being saved:</h2>

      <ul>
      <% petcategory.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
