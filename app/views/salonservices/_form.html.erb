<%= form_with(model: salonservice, local: true) do |form| %>
  <% if salonservice.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(salonservice.errors.count, "error") %> prohibited this salonservice from being saved:</h2>

      <ul>
      <% salonservice.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
