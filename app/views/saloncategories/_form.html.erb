<%= form_with(model: saloncategory, local: true) do |form| %>
  <% if saloncategory.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(saloncategory.errors.count, "error") %> prohibited this saloncategory from being saved:</h2>

      <ul>
      <% saloncategory.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :category_id %>
    <%= form.number_field :category_id %>
  </div>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :online %>
    <%= form.check_box :online %>
  </div>

  <div class="field">
    <%= form.label :deleted %>
    <%= form.check_box :deleted %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
